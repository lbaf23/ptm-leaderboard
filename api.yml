version: 1.0
provider:
  name: openfaas
  gateway: http://localhost:31112
functions:
  api:
    lang: dockerfile
    handler: ./api
    image: lbaf23/ptm-leaderboard-api:1.0.0
    build_args:
      GO111MODULE: on
    environment:
      RunMode: release
