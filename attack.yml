version: 1.0
provider:
  name: openfaas
  gateway: http://localhost:31112
functions:
  attack:
    lang: python3
    handler: ./attack
    image: lbaf23/ptm-leaderboard-attack:1.0.0
    environment:
      run_mode: release
      write_debug: true
      combine_output: false
